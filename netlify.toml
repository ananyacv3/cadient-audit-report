# Netlify build configuration
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side tab routes resolve to the prerendered index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
